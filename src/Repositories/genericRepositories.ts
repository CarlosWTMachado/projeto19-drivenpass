import db from '../Config/db';
/*
export async function findBy() {
	const query = 'SELECT * FROM db WHERE id='
	const result = await db.query<Type, [type]>(
		query, [id]
	);

	return result.rows[0];
}
*/

